import { AppShellSkeleton } from "../components/ui/page-skeletons";

export default function RootLoading() {
  return (
    <AppShellSkeleton
      title="Loading SkillSync"
      subtitle="Preparing the next view."
    />
  );
}
